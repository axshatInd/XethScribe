apiVersion: v1
kind: Service
metadata:
  name: xethscribe-service
spec:
  selector:
    app: xethscribe
  ports:
    - port: 80
      targetPort: 80
  type: LoadBalancer # For cloud deployments (use NodePort for local testing)
